For the classes defined in the ontology, introduce meaningful OWL class axioms where appropriate, including rdfs:subClassOf, owl:equivalentClass, existential restrictions (owl:someValuesFrom), universal restrictions (owl:allValuesFrom), cardinality constraints, and disjointness axioms when justified by the domain.

Add axioms only when they are semantically meaningful and supported by the ontology domain. Ensure that the resulting ontology remains logically consistent.

Apply axiomatisation to all classes for which it is appropriate.

Print only the newly created RDF/Turtle triples. Do not include explanations or repeat existing ontology content.