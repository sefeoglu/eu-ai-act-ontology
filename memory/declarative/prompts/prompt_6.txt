Considering the classes defined in the ontology, introduce meaningful OWL datatype properties where appropriate. For each datatype property, specify suitable rdfs:domain and rdfs:range axioms according to the type of value represented.

Example:

euai:hasComplianceDeadline rdf:type owl:DatatypeProperty ;
    rdfs:domain euai:Obligation ;
    rdfs:range xsd:date .

euai:requiresHumanOversight rdf:type owl:DatatypeProperty ;
    rdfs:domain euai:HighRiskAISystem ;
    rdfs:range xsd:boolean .

Modify the domain and range according to the semantics of each datatype property and the classes available in the ontology.

Print only the newly created RDF/Turtle triples. Do not include explanations or repeat existing ontology content.